# TASK-001 — Repository Foundation: Scaffold, Environment, Database Baseline, Source Registry Schema, SHA-Bound CI Evidence

**Project:** FATH_AUTOPILOT · **Task ID:** TASK-001 · **Plan version:** v6 (review sequence 2, toward attempt 3 of 3 — FINAL)
**Author role:** TASK_PLANNER (Claude Fable 5, 1M, Thinking ON, Max — runtime inherited from the Claude Fable 5 Thinking Max parent controller per control-plane model invariant)
**Status:** PLAN_READY_FOR_INDEPENDENT_REVIEW (review sequence 2, plan_review_attempt **3 of 3 — FINAL; a rejection becomes BLOCKED_FOR_SALIM per Escalation Policy** — sequence 2 authorized by Escalation Policy §6 after human-approved AMENDMENT-002 resolved FATH-P3-001; seq2 attempt-2 REJECT record: `.delivery/reviews/FATH-BOOTSTRAP/plan-review-seq2-attempt-2.md`) — NOT self-approved; authorizes no implementation until independent Sol review returns APPROVE.
**Branch when dispatched:** `task/TASK-001-repository-foundation` (cut from `main` after the controller merges the approved plan baseline; the controller records the exact cut SHA — see §6.3 A16 base identity).
**Lineage:** v2 per `REMEDIATION-1_ADJUDICATION.md`; v3 per `REMEDIATION-2_ADJUDICATION.md`; v4 per `V4_ADJUDICATION.md`; v5 per `REMEDIATION-3_ADJUDICATION.md`; v6 per `REMEDIATION-4_ADJUDICATION.md` (findings FATH-V5-001..005 corrected).

This plan is subordinate to canonical `docs/` authority — precedence: `docs/34` AMENDMENT-002 and `docs/33` AMENDMENT-001 (human-approved amendments; 34 wins over everything earlier where conflicting; the two amendments do not conflict — different domains), then `docs/24` correction layer over 00–23, then the corpus — and to `BOOTSTRAP_PLAN.md` (v6) as reviewed. It creates no new requirements.

---

## 1. Objective

Create the minimal verifiable foundation on which every later bounded task builds: doc-16-conformant repository scaffold, reproducible uv-managed Python 3.11 environment, runnable local service stack (Postgres 16 with Apache AGE + pgvector, Redis 7), typed configuration loading, Alembic migration baseline, and the Source Registry **schema** per the AMENDMENT-002 source-identity model (structure only — zero rows, no seed artifacts; §5) including mechanical `source_id` immutability enforcement, with SHA-bound CI test evidence.

## 2. Authoritative references (read before implementing)

| Authority | Governs in this task |
|---|---|
| **docs/34 AMENDMENT-002** (sha256 `0edb5245…`) | Source-identity model (SPECIFIED, §§1–11): `source_registry` single canonical table, `source_id UUID PRIMARY KEY` **immutable** (§2 — enforced in this task by a DB trigger, §4.6), `slug TEXT NOT NULL UNIQUE` (§3 — post-activation stability enforcement assigned to TASK-006, the task that introduces activation; ROADMAP + PROPAGATION_MAP PM-B2), universal semantic rule, no `sources` table, no FK to `slug` absent human-approved ADR. §13: this task's expected-schema oracle (A4), whose complete expected content is pre-bound in §6.0. §12 propagation map: `PROPAGATION_MAP.md` (binding planning material). |
| docs/33 AMENDMENT-001 | No Azure OpenAI SDK/endpoint/env in implementation/configuration (verifier item 2); no A100 assumption (item 1). This task introduces **no** LLM or GPU code. |
| docs/16 | **Canonical layout binding as written** (doc-23 anti-drift rule: deviations require an ADR WITH human approval — this task creates none). Modules: `safety/`, `budgets/`, `db/connection.py`, `db/migrations/`, `db/models/source_registry.py`, tests under `src/fath/tests/{fixtures,unit,integration}`. Root files per doc 16: `pyproject.toml`, `docker-compose.yml`, `README.md`. |
| docs/02 (as amended by 33) | Locked stack; single-Postgres decision; ADR supersession rule (human-approved per doc 23). |
| docs/22 §1 + §2 | Required extensions: uuid-ossp, vector, age; canonical table name `source_registry`. |
| docs/28 | Postgres 16, Redis 7 (running-version checks A2), Alembic migration policy and cycle test, secrets rules (.env local only). |
| docs/03 | SourceRegistryRecord field contract (fields, types, Pydantic defaults, numeric boundaries — transcribed into the §6.0 pre-bound schema and A6), registry indices (exact names, §6.0). Seed VALUES not established — §5, FA-OPEN-020. |
| docs/04 "Foreign keys"; docs/21 DDL | `raw_archive.source_id → source_registry.source_id`; `source_id UUID NOT NULL REFERENCES source_registry(source_id)` — surviving UUID contracts (AMENDMENT-002 §10) the schema must keep satisfiable. This slice itself contains **zero** FKs (§6.0). |
| docs/24 §1 | Source status enum + DB CHECK + DEFAULT 'candidate' — applies to `source_registry` per AMENDMENT-002 §1 (no `sources` table). Week-1 textual sets are slugs per §11. |
| docs/29, docs/30 | Compliance-table DDL superseded per AMENDMENT-002 §7 (UUID FKs → `source_registry(source_id)`, created in TASK-006); textual ids are slugs (§11); YAML keys on `slug:` (§8). Doc-30 Tier 0–3 slugs enter the A13a literal list (§5). Recorded as forward contracts — nothing from docs 29/30 is created in this task. |
| docs/23 | Week-1 done criterion "Source Registry table exists"; anti-drift rule (ADR + human approval). |
| Control plane: constitution §§9/19, review policy §§36–46, Oracle Policy §§5/7/10/11/37/48, SG-TR-006/007 | CI as SHA-bound evidence; candidates halt at REVIEW_APPROVED until the trusted gate is verified active (§11); oracle independence, boundary triplets, negative evidence, claim precision; data classification; uv; typing. |
| BOOTSTRAP_PLAN.md (v5) §§4, 6, 7.4, 9, 10.6, 12 | Identity model (§7.4 → PROPAGATION_MAP.md), PROPOSED items (§4.4), dependency baseline (§6), CI + GATE-SETUP sequencing (§9), **evidence retention model (§10.6 — governs §10 here)**. |
| Official uv documentation (docs.astral.sh/uv — Locking and syncing) | `--frozen` uses the lockfile WITHOUT checking freshness; `uv lock --check` (equivalent to `--locked`) errors when `uv.lock` does not match `pyproject.toml`. Basis for A1/A15 (FATH-V4-004). |

## 3. Requirements traced (from REQUIREMENTS_TRACEABILITY.json)

FA-REQ-W1-001 (registry **schema + indices** portion; seed data = TASK-006 gated on FA-OPEN-020), FA-REQ-W1-002 (status enum + CHECK — schema portion), FA-REQ-W1-018 (doc-16 layout), FA-REQ-W1-019 (PG16 + extensions; Redis 7), FA-REQ-W1-020 (Alembic cycle), FA-REQ-AM-003 (AMENDMENT-002 identity model + §13 oracle + §2 immutability enforcement), FA-REQ-CP-001 (CI as SHA-bound evidence), FA-REQ-CP-002 (uv, typing), FA-REQ-CP-003 (governed workflow; halt at REVIEW_APPROVED pre-gate), FA-REQ-AM-001 (negative constraint: no Azure OpenAI artifact in implementation/configuration).

## 3a. Data classification (SG-TR-006 — FATH-V4-008)

**Classification: PUBLIC / OWNER-AUTHORED. No restricted data.** Everything in TASK-001 scope is one of: (i) the owner's own public specification corpus (`docs/`, Salim-authored, non-client material); (ii) planner/controller delivery artifacts under `.delivery/`; (iii) **synthetic fixtures only**, mechanically constrained by A13d (RFC-2606 domains, `synthetic_` slugs, zero authoritative literals); (iv) open-source tool configuration and outputs. No client data, no personal data, no production data, no unrestricted client datasets, and no credentials enter this task or any model context. Handling restrictions: real seed-source values remain FA-OPEN-020-gated and are mechanically excluded (A12/A13); secrets are never committed (`.env` git-ignored, A9 gitleaks, `.env.example` placeholders only). Because no restricted data is in scope, unattended autonomous execution of this task is permitted under SG-TR-006.

## 4. Scope (what must change)

1. **Repository scaffold — doc-16 canonical layout** for every path created (full anchored path oracle: §6.1/A5): `src/fath/__init__.py`; packages `config/`, `db/` (`connection.py`, `migrations/`, `models/`), `memory/`, `safety/`, `crawlers/`, `parsers/`, `extractors/`, `graph/`, `embeddings/`, `agents/`, `validators/`, `workflows/`, `events/`, `budgets/`, `ui/`, `api/`; tests under `src/fath/tests/{fixtures,unit,integration}`; root `pyproject.toml`, `docker-compose.yml`, `README.md`. Disclosed root-level additions (not module paths): `.gitignore`, `.env.example`, `alembic.ini`, `Makefile` (test/lint/typecheck targets only — PROPOSED), `docker/postgres/Dockerfile` (PROPOSED packaging, §4.4), `.github/workflows/ci.yml`, `.github/scan/authoritative_source_literals.json` (A13a scan input — outside the fixtures tree so A13d applies to fixtures unconditionally; FATH-V4-003), optional `.python-version` (content `3.11`). `docs/` untouched; `.delivery/` written ONLY under `.delivery/evidence/TASK-001/` (§10 — the single authorized exception, mechanically enforced by A16).
2. **Python environment:** `pyproject.toml` with `requires-python = ">=3.11,<3.12"` (Python 3.11 PROPOSED baseline, BOOTSTRAP_PLAN §4.4) + committed `uv.lock` whose freshness is proven by `uv lock --check` (A1). Direct dependencies exactly per the §4.9 REQUIRED/OPTIONAL/PROHIBITED contract. No FastAPI, LangGraph, Prefect, or any LLM/embedding dependency.
3. **Tooling config:** ruff (lint + format), mypy strict on `src/`, pytest config.
4. **Local service stack (PROPOSED packaging per BOOTSTRAP_PLAN §4.4):** `docker/postgres/Dockerfile` — Postgres 16 base pinned by digest; AGE and pgvector pinned to exact release tags recorded in the Dockerfile. `docker-compose.yml` with that image + **Redis 7 pinned by immutable digest** (`redis:<7.x.y>@sha256:<digest>` — a bare tag like `redis:7` is mutable and non-reproducible; FATH-V5-004; the implementer resolves a current Redis 7 release digest and records it in the compose file; A2 independently verifies running major 7); named volumes; healthchecks.
5. **Typed configuration:** `src/fath/config/settings.py` (pydantic-settings) loading `DATABASE_URL`, `REDIS_URL`, `FATH_ENV` only; `.env.example` with placeholders for exactly those keys — no Azure OpenAI variable, no frontier-provider key (those enter with the Week-2 LLM-client task).
6. **Database baseline:** async SQLAlchemy engine/session factory in `db/connection.py`; Alembic with `script_location = src/fath/db/migrations`; **migration 0001** — DDL only, zero DML (enforced by the A13b-ii AST-allowlist detector over migration files): enable the three extensions and create `source_registry` **exactly per the §6.0 pre-bound expected schema** (AMENDMENT-002 §§1–3 + docs/03 + docs/24 §1). The migration is a transcription of §6.0 — it introduces no schema element not listed there and omits none. Includes:
   - `source_id UUID PRIMARY KEY DEFAULT uuid_generate_v4()` (type/PK SPECIFIED docs/34 §2; the server-side default is PROPOSED per BOOTSTRAP_PLAN §4.4 and **pre-bound in §6.0** — the fixture no longer pins "whichever form the migration declares"; FATH-V4-002).
   - **`source_id` immutability enforcement (docs/34 §2 invariant; trigger mechanism PROPOSED — FATH-V4-001):** function `fath_source_id_immutable()` + trigger `trg_source_registry_source_id_immutable` BEFORE UPDATE ON `source_registry` FOR EACH ROW, raising an exception whenever `NEW.source_id IS DISTINCT FROM OLD.source_id`. Verified structurally and negatively by A4 (ix)/(x).
   - `slug TEXT NOT NULL UNIQUE` (docs/34 §3). **Slug post-activation stability (docs/34 §3) is NOT enforceable in this slice** (activation workflow does not exist yet; there are zero rows): its mechanical enforcement — a trigger forbidding `slug` UPDATE on rows whose `status` is not `candidate`/`candidate_manual_review` except through the audited-migration path TASK-006 defines, with negative tests — is **assigned to TASK-006** as a binding acceptance obligation (ROADMAP TASK-006; PROPAGATION_MAP PM-B2).
   - All remaining columns, constraints (exact names), indices, defaults exactly per §6.0.
   - **The migration inserts zero rows and contains zero DML statements.** Full downgrade implemented. **No `sources` table. No other table** — the doc-29 compliance tables arrive with TASK-006 and will FK `source_id UUID REFERENCES source_registry(source_id)` per docs/34 §7 (forward contract, PROPAGATION_MAP PM-C3/C4; not created now).
7. **Pydantic contract:** `db/models/source_registry.py` — `SourceRegistryRecord` per the doc-03 field contract verbatim (`source_id: UUID` with uuid4 default) **plus** `slug: str` (SPECIFIED docs/34 §§3/5) and the doc-24 §1 `status` field (SPECIFIED). This module contains contract models only — no engine/session import (enforced by A13c import discipline).
8. **CI (SHA-bound evidence):** `.github/workflows/ci.yml` per BOOTSTRAP_PLAN §9, step order fixed in §4.10 below. **Classification: candidate-controlled test evidence bound to the exact SHA — never trusted verification. Trusted gates are NOT_CONFIGURED / branch protection NOT_VERIFIED (BUILD_STATE); this candidate halts at REVIEW_APPROVED and is NOT merge-eligible until the §11 gate sequencing completes.**
9. **Dependency/environment contract (three disjoint sets; mechanically checked by A15 — FATH-V4-004):**
   - **REQUIRED direct `[project.dependencies]` (must ALL be declared — positive completeness):** `pydantic` (v2), `pydantic-settings`, `sqlalchemy` (2.x), `alembic`, `asyncpg`, `redis`, `pyyaml`.
   - **REQUIRED direct dev-dependencies (must ALL be declared):** `pytest`, `pytest-asyncio`, `ruff`, `mypy`, `types-pyyaml`.
   - **OPTIONAL permitted direct (may be declared, nothing else may):** `psycopg[binary]` (sync driver if Alembic offline/online split needs it), `testcontainers`, `greenlet` (SQLAlchemy async runtime requirement, if the resolver does not pull it automatically).
   - **PROHIBITED anywhere in `pyproject.toml` or `uv.lock` (name match, covers transitives):** `fastapi`, `langgraph`, `prefect`, `openai`, `azure-openai`, any `azure-*` SDK, `anthropic`, `transformers`, `torch`, `sentence-transformers`, `vllm`, `unstructured`, `paddleocr`.
   - **Pre-bound version contract (FATH-V5-003):** the declared specifiers in `pyproject.toml` MUST be exactly `pydantic>=2,<3`, `pydantic-settings>=2,<3`, `sqlalchemy>=2,<3` (string equality after whitespace normalization — these are this plan's own §4.9 contract values: pydantic v2, SQLAlchemy 2.x, with pydantic-settings v2 as the line that pairs with pydantic v2); AND the resolved versions recorded in `uv.lock` for `pydantic`, `pydantic-settings`, and `sqlalchemy` MUST have major version 2 (mechanical parse of the `[[package]]` name/version entries). Other REQUIRED/OPTIONAL dependencies carry no major-version contract in this plan (no authority or plan constraint binds them); their integrity is name + lock-freshness only, disclosed as scope.
   - **A15 check (fully mechanical):** REQUIRED ⊆ declared ∧ declared ⊆ (REQUIRED ∪ OPTIONAL) for each of the two dependency groups (exact-name comparison after PEP-503 normalization), ∧ the three pre-bound specifier strings match exactly ∧ the three resolved `uv.lock` majors equal 2, ∧ zero prohibited-name hits over `pyproject.toml` + `uv.lock`. Omission of any REQUIRED name FAILS (the v4 subset-only check passed on omission — corrected); a name-correct but wrong-major declaration or resolution FAILS (the v5 name-only check passed on it — corrected).
   - Python: `requires-python = ">=3.11,<3.12"`; CI asserts the running interpreter is 3.11.x (A14).
   - Transitive resolution is uv's (limitation stated per Oracle Policy §48 — the prohibited-name scan over `uv.lock` also covers transitives).
10. **CI step order (deterministic; identical for push and PR events, bound to exact SHA; every third-party GitHub Action pinned by full commit SHA — mutable tags like `@v4` are the same defect class as mutable image tags, FATH-V5-004 sweep):**
    1. checkout candidate SHA with full history (`fetch-depth: 0` — required by step 4); assert `python --version` is 3.11.x (A14);
    2. **`uv lock --check`** (lockfile-freshness proof — official uv docs: equivalent to `--locked`; `--frozen` does NOT verify freshness) then **`uv sync --locked`** (A1) + dependency-contract completeness AND version-contract check (A15 — names, exact specifier strings, resolved lock majors);
    3. `ruff check` + `ruff format --check`; `mypy src/` (A7);
    4. diff-boundary check (A16): read `.delivery/evidence/TASK-001/BASE_SHA.txt`; assert it is a 40-hex commit reachable as ancestor (`git merge-base --is-ancestor`); `git diff --name-status --no-renames <base> HEAD`; apply the §6.3 rules;
    5. build pinned PG image; start postgres (built image) + redis (digest-pinned image) services; running-version checks (A2);
    6. `alembic upgrade head` → **zero-row check (A12a)**;
    7. expected-schema comparison (A4 — including table-set equality, FK-count-zero, trigger presence, full index-definition equality) + functional extension checks (A11);
    8. `pytest` (unit + integration, against this database; includes the A4 negative tests, the source_id-immutability negative test, and the migration-DML detector self-test against the negative fixtures) → **post-test zero-row check (A12b)**;
    9. `alembic downgrade base` → `alembic upgrade head` → **zero-row check (A12c)** (doc-28 cycle = A3);
    10. expected-tree check (A5); seed-boundary checks: authoritative-literal scan (A13a), registry-write scan + **migration DML detector explicit invocation** (`pytest src/fath/tests/unit/test_migration_dml_discipline.py`) (A13b), import discipline (A13c), fixture-content rules (A13d);
    11. scoped AMENDMENT-001 scan (A8); gitleaks scan — tool pinned by exact version with recorded sha256 checksum of the downloaded binary (A9).
    Fails on any error; no network calls in tests; fixed seeds where randomness exists.

## 5. Seed data — descoped AND negatively enforced (FATH-PR-001, FATH-P2-002, FATH-P3-003, FATH-V4-003)

Authority establishes seed identities (as slugs per docs/34 §11) but not the required values (`base_url`, `reliability_tier` — mandatory doc-03 fields with no defaults — plus per-source rate limits and collection modes), and doc 29 requires human-approved onboarding checklists before activation. Seeds are therefore TASK-006 scope, gated on **FA-OPEN-020** (Salim-approved seed value table + Tier-0 checklists; dispatch without it is `BLOCKED_FOR_SALIM`).

For THIS candidate the boundary is mechanically enforced by checks that do not depend on any candidate-authored attestation:

- **A12 zero-seed state, three measurement points:** `SELECT COUNT(*) FROM source_registry` = 0 (a) after fresh `upgrade head` before tests, (b) **after the full pytest session against the same database** — this directly proves no test persisted a row (fixture non-persistence established by observation, not by marker; tests that must insert rows for negative checks do so inside rolled-back transactions), and (c) after the downgrade/upgrade cycle. Run as CI-level SQL independent of pytest.
- **A13a authoritative-literal scan:** over every tracked file in the candidate (`git ls-files`, minus `docs/**`, `.delivery/**`, and the scan-input file itself at `.github/scan/authoritative_source_literals.json` — its self-exemption is definitional: a checker input must contain the patterns it checks for; it lives OUTSIDE the fixtures tree so the A13d fixture rule stays unconditional; FATH-V4-003), a word-boundary case-insensitive scan for the authoritative source-identity literals must return zero hits. **The complete literal list is pre-bound by this plan** (the committed scan file is a transcription, not implementer-authored content):
  - doc-24 §1 slugs (7): `qatar_open_data`, `world_bank`, `gdelt`, `al_meezan`, `qcb`, `qse`, `invest_qatar`;
  - doc-30 Tier-0 slugs (3, overlap with above): `qatar_open_data`, `world_bank`, `gdelt`;
  - doc-30 Tier-1 slugs (19): `national_planning_council_psa`, `ministry_of_commerce_industry`, `ministry_of_finance`, `ministry_of_communications_it`, `qatar_central_bank`, `qatar_stock_exchange`, `invest_qatar`, `qatar_financial_centre`, `qatar_free_zones_authority`, `qatar_development_bank`, `qatar_chamber`, `imf`, `un_comtrade`, `wits`, `ilostat`, `escwa`, `unctad`, `wto`, `world_economic_forum_reports`;
  - doc-30 Tier-2 slugs (7): `al_meezan`, `qatar_energy_publications`, `mwani_qatar_or_ports`, `qatar_airways_public_reports`, `qatar_tourism`, `qatar_university_qf_publications`, `hamad_medical_or_health_reports`;
  - **doc-30 Tier-3 slugs (8 — omitted in v4, FATH-V4-003):** `fdi_markets`, `refinitiv_or_lseg`, `factset`, `orbis_bvd`, `pitchbook_or_cb_insights`, `itc_trade_map`, `s_and_p_capital_iq`, `planet_or_maxar`;
  - doc-03 seed display names (16): `Qatar Open Data`, `National Planning Council / PSA`, `Al Meezan`, `World Bank`, `IMF`, `UN Comtrade`, `WITS`, `ILOSTAT`, `ESCWA`, `Qatar Central Bank`, `Qatar Stock Exchange`, `Invest Qatar`, `GDELT`, `UNCTAD`, `WTO`, `GCC-Stat`.
  Deduplicated: 39 slugs + 16 display names = **55 literals** — every governed source identity in docs 03/24/30, all tiers. This detects production seed identities **by value, in ANY file under ANY variable/file name**. The root `README.md` is inside the scan domain; its content must therefore avoid seed-source names (disclosed constraint).
- **A13b registry-write and migration-DML discipline (loader-behavior detection — FATH-V4-003, detector rebuilt per FATH-V5-001):** in this bounded candidate NO code writes rows to `source_registry` — including migration 0001 (DDL-only). Mechanical enforcement, two layers:
  - (b-i) every tracked file under `src/` EXCLUDING `src/fath/tests/**` and `src/fath/db/migrations/**` must contain no reference to `source_registry` in combination with INSERT/write patterns (SQL `INSERT`, SQLAlchemy `insert(`, `.add(`, `executemany`, `COPY`);
  - (b-ii) **migration DML detector — allowlist design, not DML-form enumeration (FATH-V5-001):** the v5 regex layer missed ordinary SQLAlchemy/Alembic API forms (`op.execute(sa.insert(t).values(…))`, `op.execute(sa.delete(t))` — both verified to produce zero hits against the v5 patterns). Replaced by an AST-level detector (Python `ast` parse; comments/formatting-immune) with a **pre-bound allowlist**: migrations may contain schema DDL operations and NOTHING else. Rules, all mechanical:
    - **R1 (op-call allowlist; scope `src/fath/db/migrations/versions/*.py`):** every call `op.<attr>(…)` must have `attr` ∈ {`create_table`, `drop_table`, `create_index`, `drop_index`, `create_check_constraint`, `drop_constraint`, `execute`}. Anything else — `bulk_insert`, `get_bind`, etc. — FAILS.
    - **R2 (op.execute literal-DDL rule; same scope):** every `op.execute(…)` must take exactly one positional argument that is a plain string literal (no expression objects — `sa.insert(…)`/`t.insert()` FAIL here), and that string must begin with a DDL keyword: `^\s*(CREATE|DROP|ALTER|COMMENT)\b` case-insensitive. The string is NOT split on `;` (PL/pgSQL trigger bodies legitimately contain internal semicolons); DML smuggled after a DDL prefix is caught by R4.
    - **R3 (prohibited names; scope ALL files under `src/fath/db/migrations/**` including `env.py`):** no identifier or attribute named `insert`, `update`, `delete`, `bulk_insert`, `executemany`, `exec_driver_sql`, `get_bind`, `merge`, `copy_expert`, or `copy_from` anywhere in the AST (`sa.text` is deliberately NOT prohibited: it is not a DML vector by itself — any DML content inside any string is caught by R4, and every execution path is closed by R1/R2/R3).
    - **R4 (string-content DML scan; same scope as R3):** no string literal anywhere in a migration file may match `\bINSERT\s+INTO\b`, `\bUPDATE\s+\w+\s+SET\b`, `\bDELETE\s+FROM\b`, `\bTRUNCATE\b`, `\bCOPY\s+\w+\s+FROM\b`, `\bMERGE\s+INTO\b` (case-insensitive). DDL such as `CREATE TRIGGER … BEFORE UPDATE ON …` and `RAISE EXCEPTION '…'` does not match.
    - **Negative fixtures (the detector must be PROVEN to fail on the named evasions):** the candidate commits, under `src/fath/tests/fixtures/negative_migrations/` as `*.py.sample` files (not collected by pytest/mypy/ruff; obey A13d content rules — synthetic identifiers only), at least: N1 `op.execute(sa.insert(t).values(…))` + `op.execute(sa.delete(t))` (the reviewer-cited form); N2 `op.bulk_insert(…)`; N3 `op.execute("INSERT INTO …")`; N4 `conn = op.get_bind(); conn.execute(…)`; N5 `op.execute(t.insert().values(…))`; N6 DML appended after a DDL prefix inside one `op.execute` string. The detector lives in `src/fath/tests/unit/test_migration_dml_discipline.py`, which asserts (a) every real file under `db/migrations/` passes and (b) **every negative fixture FAILS** — an insert-then-delete migration is thereby demonstrated, not assumed, to be detected. Runs in the pytest session (step 8) and as an explicit step-10 invocation. This detector design was executed against all six fixtures and a realistic migration-0001 shape (extensions + table + indexes + PL/pgSQL trigger) during planning: all six fixtures fail (each caught by ≥1 rule, most by ≥2), the legitimate migration passes.
- **A13c import discipline:** database-access imports (`sqlalchemy`, `asyncpg`, `psycopg`, `alembic`) are permitted ONLY in `src/fath/db/connection.py`, `src/fath/db/migrations/**`, and `src/fath/tests/**`; every `src/fath/**/__init__.py` in this candidate must be empty or docstring-only; `src/fath/db/models/source_registry.py` imports pydantic/stdlib only. A loader "under a neutral name" cannot exist in this candidate without violating A13b or A13c: any effective loader needs both registry-write statements and DB access. Scans are mechanical (pattern-based); their residual limitation against deliberate obfuscation (e.g. encoded literals, dynamic imports) is stated per Oracle Policy §48 and is covered by the independent reviewer's inspection of the actual diff — the checks make the covert paths named by the reviewer mechanically detectable, and active obfuscation would itself be reviewable evidence of bad faith.
- **A13d fixture syntheticity (content rules, not attestation — now unconditional):** every file under `src/fath/tests/fixtures/` must (i) contain zero A13a authoritative literals — **no exemption exists any more; the scan-input file lives outside the fixtures tree (FATH-V4-003)**, (ii) use only RFC-2606 reserved domains (`example.com/net/org`, `.test`, `.invalid`) in any URL value, and (iii) any `slug` value must match `^synthetic_[a-z0-9_]+$`. These are mechanical content rules that establish syntheticity independently of any marker. The v3 `SYNTHETIC-TEST-FIXTURE` marker remains as labeling hygiene but is **not load-bearing evidence**.
- **No seed artifact paths:** no `src/fath/config/sources_seed.yaml` (in A5's PROHIBITED set for this candidate even though doc 16 lists it — it is TASK-006 scope), no `execution_rules.yaml`, no `*seed*` path under `src/` (`git ls-files` scan).

## 6. Acceptance criteria and oracles

### 6.0 Pre-bound expected schema (the complete A4 oracle content — fixed by this plan; FATH-V4-002)

The committed fixture `src/fath/tests/fixtures/expected_schema.json` is a **transcription of this section** — the implementer transcribes, and does not choose, any expected value. Mapping classifications: column set and semantics SPECIFIED (docs/03; docs/34 §§2–3; docs/24 §1); PG type mapping DERIVED per the rules stated here (enum→TEXT+CHECK follows the doc-24 §1 explicit TEXT+CHECK pattern; `AnyUrl`→TEXT; `list[str]`→TEXT[] per the doc-22 `aliases TEXT[]` precedent; `dict`→JSONB; `datetime`→TIMESTAMPTZ; `confloat`→DOUBLE PRECISION; `conint`→INTEGER) except where marked PROPOSED; DB-level CHECK mirroring of doc-03 numeric/enum bounds is DERIVED (the bounds are doc-03 authority; expressing them also at DB level is the schema-side half, with Pydantic-side verification in A6).

**Table set (exact):** after `alembic upgrade head`, the set of ordinary tables (`relkind='r'`) in schema `public` equals exactly `{alembic_version, source_registry}`. (Extension objects live outside `public`: AGE in `ag_catalog`; pgvector/uuid-ossp add no `public` tables.) Any additional application table FAILS — this fixes the allowed-table boundary the v4 plan left open.

**Columns of `source_registry` (exact set of 33 — name, PG type, nullability, DB default in `pg_get_expr` normalized form; every column NOT listed with a default must have `adbin IS NULL`):**

| # | Column | Type | NOT NULL | DB default (normalized) |
|---|---|---|---|---|
| 1 | `source_id` | uuid | YES | `uuid_generate_v4()` (PROPOSED mechanism; docs/34 §8 permits generated-or-assigned) |
| 2 | `slug` | text | YES | — |
| 3 | `name` | text | YES | — |
| 4 | `source_class` | text | YES | — |
| 5 | `reliability_tier` | text | YES | — |
| 6 | `base_url` | text | YES | — |
| 7 | `api_base_url` | text | NO | — |
| 8 | `robots_url` | text | NO | — |
| 9 | `terms_url` | text | NO | — |
| 10 | `access_method` | text | YES | — |
| 11 | `auth_requirement` | text | YES | — |
| 12 | `subscription_name` | text | NO | — |
| 13 | `allowed_paths` | text[] | YES | — |
| 14 | `disallowed_paths` | text[] | YES | — |
| 15 | `robots_status` | text | YES | — |
| 16 | `max_requests_per_minute` | integer | YES | — |
| 17 | `max_pages_per_cycle` | integer | YES | — |
| 18 | `max_bytes_per_cycle` | bigint (PROPOSED: byte magnitudes need int8 headroom) | YES | — |
| 19 | `language_codes` | text[] | YES | — |
| 20 | `country_scope` | text[] | YES | — |
| 21 | `topic_scope` | text[] | YES | — |
| 22 | `update_frequency_hint` | text | YES | — |
| 23 | `independence_group` | text | NO | — |
| 24 | `reliability_prior` | double precision | YES | — |
| 25 | `strategic_relevance_score` | double precision | YES | — |
| 26 | `data_quality_notes` | text | NO | — |
| 27 | `legal_notes` | text | NO | — |
| 28 | `enabled` | boolean | YES | — |
| 29 | `last_access_review_at` | timestamptz | NO | — |
| 30 | `created_at` | timestamptz | YES | — |
| 31 | `updated_at` | timestamptz | YES | — |
| 32 | `metadata` | jsonb | YES | — |
| 33 | `status` | text | YES | `'candidate'::text` (SPECIFIED docs/24 §1) |

Nullability rule applied: doc-03 `Optional[...] = None` fields → NULL (rows 7–9, 12, 23, 26–27, 29); every other field NOT NULL. Pydantic-layer defaults (e.g. `max_requests_per_minute = 30`) are deliberately NOT DB defaults — they are app-layer contract, verified by A6; only rows 1 and 33 carry DB defaults, and the fixture asserts the ABSENCE of a default on all 31 other columns.

**Constraints (exact names, exact definitions):**

- `source_registry_pkey` — PRIMARY KEY (`source_id`).
- `source_registry_slug_key` — UNIQUE (`slug`).
- `ck_source_registry_status` — CHECK `status IN ('candidate','candidate_manual_review','approved_inactive','active','suspended','quarantined','retired')` (docs/24 §1, exactly 7 values).
- `ck_source_registry_source_class` — CHECK `source_class IN ('government_open_data','legal_corpus','global_indicator','trade_data','financial_disclosure','investment_signal','news_event','benchmark_country','report_library')` (doc-03 SourceClass, 9 values).
- `ck_source_registry_reliability_tier` — CHECK `reliability_tier IN ('primary','official_secondary','institutional','media','low_confidence')` (5 values).
- `ck_source_registry_access_method` — CHECK `access_method IN ('api','bulk_download','rss','sitemap','polite_crawl','manual_ingestion','disabled')` (7 values).
- `ck_source_registry_auth_requirement` — CHECK `auth_requirement IN ('none','api_key','paid_subscription','login_required','not_allowed')` (5 values).
- `ck_source_registry_robots_status` — CHECK `robots_status IN ('allowed','disallowed','partial','not_applicable','unknown')` (5 values).
- `ck_source_registry_max_requests_per_minute` — CHECK `(max_requests_per_minute >= 0)`; `ck_source_registry_max_pages_per_cycle` — CHECK `(max_pages_per_cycle >= 0)`; `ck_source_registry_max_bytes_per_cycle` — CHECK `(max_bytes_per_cycle >= 0)` (doc-03 `conint(ge=0)` bounds — these are the numeric schema boundaries, now DB-expressed).
- `ck_source_registry_reliability_prior` — CHECK `(reliability_prior >= 0 AND reliability_prior <= 1)`; `ck_source_registry_strategic_relevance_score` — CHECK `(strategic_relevance_score >= 0 AND strategic_relevance_score <= 1)` (doc-03 `confloat(ge=0, le=1)` bounds).
- The fixture asserts the constraint SET on `source_registry` equals exactly the above — **13 named constraints** (PK, UNIQUE, 6 enum CHECKs, 5 numeric-boundary CHECKs) — no extra, none missing.
- **Constraint comparison method (definition-level, not name-only):** for each constraint, compare `contype` and name, then the DEFINITION: for the two enum-set styles, extract the value set from `pg_get_constraintdef(oid)` and assert set equality against the pre-bound value lists above; for the five numeric CHECKs, extract the bound expressions and assert equality against the pre-bound bounds; for PK/UNIQUE, resolve `conkey` to column names and assert exact column lists (`source_id`, `slug`). Structured extraction is used here (rather than byte-exact rendered-string equality) because Postgres normalizes `IN` lists to `= ANY (ARRAY[…])` renderings whose byte form is server-formatting, while the value sets and bounds are the schema contract; every expected value remains pre-bound in this section. A name-correct constraint over the wrong column/values FAILS.

**Foreign keys (exact expectation — no vacuous universal):** this slice contains **ZERO** FK constraints. A4 asserts positively: `SELECT count(*) FROM pg_constraint WHERE contype = 'f' AND connamespace = 'public'::regnamespace` **= 0**. The standing rule "no FK anywhere references `source_registry(slug)`; every FK referencing `source_registry` targets `source_id`" is additionally executed as a query (returns empty now BECAUSE the FK count is zero — the count assertion is what carries evidential weight in this slice) and re-runs with real content in every owning task per PROPAGATION_MAP PM-C7.

**Indexes (exact set AND exact definitions — FATH-V5-002):** the index set on `source_registry` equals exactly the six below — the four doc-03 index names verbatim (SPECIFIED: names and indexed columns from the doc-03 `CREATE INDEX` statements) plus the two constraint-backed indexes; access method btree (DERIVED: doc-03 uses plain `CREATE INDEX`, whose Postgres default is btree), single-column, no predicates, no expression indexes, default ordering. (The two doc-03 `access_decisions` indices belong to TASK-006.) A4 compares by **normalized `pg_get_indexdef(indexrelid)` string equality** against the following pre-bound strings (same style as the `pg_get_expr` default binding) — a name-correct index over the wrong column, method, uniqueness, or with a predicate FAILS:

1. `CREATE UNIQUE INDEX source_registry_pkey ON public.source_registry USING btree (source_id)`
2. `CREATE UNIQUE INDEX source_registry_slug_key ON public.source_registry USING btree (slug)`
3. `CREATE INDEX idx_sources_enabled ON public.source_registry USING btree (enabled)`
4. `CREATE INDEX idx_sources_class ON public.source_registry USING btree (source_class)`
5. `CREATE INDEX idx_sources_reliability ON public.source_registry USING btree (reliability_tier)`
6. `CREATE INDEX idx_sources_independence_group ON public.source_registry USING btree (independence_group)`

Normalization before comparison: collapse whitespace runs to single spaces and strip. If the running server renders a definition in a byte-form that differs from the pre-bound string while being semantically identical, that is a §9 stop condition (fix the pre-bound fixture via governed correction, never pin from output silently).

**Triggers (exact):** exactly one row-level trigger on `source_registry`: `trg_source_registry_source_id_immutable`, BEFORE UPDATE, executing `fath_source_id_immutable()` (docs/34 §2 immutability; mechanism PROPOSED). Verified via `pg_trigger` (name, table, timing, level, not disabled).

**Extensions:** `pg_extension` ⊇ {`uuid-ossp`, `vector`, `age`} (docs/22 §1).

### 6.1 A5 expected-tree oracle (anchored four-set manifest over the normalized Git tree — FATH-P2-003, FATH-P3-004)

**Scan domain (fixed):** the set `P` of file paths returned by `git ls-files` at the exact candidate SHA (the normalized tracked Git tree — file paths, forward slashes, repo-root-relative). Filesystem state is OUT of domain: `.venv`, caches, tool byproducts, and untracked files never enter `P` because they are not tracked; CI runs the check on a clean checkout of the candidate SHA. Two path prefixes are carved out of `P` before comparison: `docs/**` (read-only authority; A16 proves untouched) and `.delivery/**` (governed separately by A16: only `.delivery/evidence/TASK-001/**` may be ADDED by this candidate).

**Manifest:** the candidate commits `src/fath/tests/fixtures/expected_tree.json` — a transcription of the four sets below (pre-bound by this plan; not generated from the working tree) — in which **every entry is a full anchored path or an anchored glob rooted at an authorized parent path. There are no unanchored patterns: a bare `__init__.py` or `*.py` pattern is prohibited in the manifest itself** (FATH-P3-004).

- **REQUIRED** (each entry cites doc 16 or this plan §4): `pyproject.toml`, `uv.lock`, `docker-compose.yml`, `README.md`, `.github/workflows/ci.yml`, `.github/scan/authoritative_source_literals.json`, `docker/postgres/Dockerfile`, `alembic.ini`, `.gitignore`, `.env.example`, `src/fath/__init__.py`, for each module M ∈ {config, db, db/models, memory, safety, crawlers, parsers, extractors, graph, embeddings, agents, validators, workflows, events, budgets, ui, api}: `src/fath/<M>/__init__.py`, plus `src/fath/config/settings.py`, `src/fath/db/connection.py`, `src/fath/db/models/source_registry.py`, `src/fath/db/migrations/env.py`, exactly one `src/fath/db/migrations/versions/0001_*.py`, `src/fath/tests/fixtures/expected_schema.json`, `src/fath/tests/fixtures/expected_tree.json`, `.delivery/evidence/TASK-001/BASE_SHA.txt` (checked via the A16 diff, not the A5 carve-out domain).
- **PERMITTED** (anchored; may exist): `Makefile`, `.python-version`, `src/fath/db/migrations/__init__.py`, `src/fath/db/migrations/versions/__init__.py`, `src/fath/db/migrations/script.py.mako`, `src/fath/tests/__init__.py`, `src/fath/tests/{fixtures,unit,integration}/__init__.py`, `src/fath/tests/unit/test_*.py`, `src/fath/tests/integration/test_*.py`, `src/fath/tests/fixtures/*.json`, `src/fath/tests/fixtures/*.yaml`, `src/fath/tests/fixtures/negative_migrations/__init__.py` ABSENT and `src/fath/tests/fixtures/negative_migrations/*.py.sample` (the A13b-ii detector negative fixtures — `.py.sample` so they are never collected by pytest, mypy, ruff, or alembic; subject to A13d content rules like all fixtures), tool configs at root only: `ruff.toml`, `mypy.ini`, `pytest.ini`, `conftest.py` (root or under `src/fath/tests/`), `docker/postgres/*.sql`/`*.sh` init files.
- **DEFERRED** (canonical in doc 16 but NOT created by TASK-001; absence must not fail; presence outside PERMITTED fails): `frontend/**`, doc-16 per-module implementation files (e.g. `src/fath/crawlers/base.py`, `src/fath/safety/trust_boundary.py`, `src/fath/db/models/raw_archive.py`, …), `src/fath/config/sources_seed.yaml`, `src/fath/config/execution_rules.yaml`, `src/fath/safety/injection_patterns.yaml`, `docs/adr/**`, `golden/**`, `scripts/**`.
- **PROHIBITED** (must NOT exist; anchored): any path matching `src/fath/*/` whose first segment under `src/fath/` is not in the doc-16 module list above (this check is computed from `P`, so e.g. `rogue/__init__.py` or `src/fath/llm/__init__.py` fails), top-level `tests/**` or `migrations/**`, `src/fath/config/sources_seed.yaml`, any path matching `*seed*` under `src/`, any second migration version file.

**Check (mechanical):** (REQUIRED ⊆ P) ∧ (P ∩ PROHIBITED = ∅) ∧ (every p ∈ P after carve-outs is matched by REQUIRED ∪ PERMITTED). Because every REQUIRED/PERMITTED entry is anchored, any noncanonical path — including a stray `__init__.py` anywhere outside the enumerated parents — fails the third condition.

### 6.2 Criteria table

| # | Criterion | Oracle class / verification |
|---|---|---|
| A1 | **`uv lock --check` succeeds** (proves `uv.lock` matches `pyproject.toml` — official uv docs: `--frozen` does NOT check freshness; FATH-V4-004) **then `uv sync --locked` succeeds** from clean checkout; `uv.lock` committed | SPECIFIED (SG-TR-007 + uv official semantics); CI step |
| A2 | Services healthy AND **running versions verified**: `docker compose up -d` yields healthy postgres + redis; after migration `SELECT extname FROM pg_extension` ⊇ {uuid-ossp, vector, age}; `SELECT current_setting('server_version_num')::int` in [160000, 170000) (Postgres major 16); Redis `INFO server` reports `redis_version` major 7 | SPECIFIED (docs 22 §1, 28); integration test + CI (FATH-P3-002) |
| A3 | `alembic upgrade head` → `downgrade base` → `upgrade head` all succeed (doc-28 cycle) | SPECIFIED (doc 28); CI step |
| A4 | **Expected-schema oracle (docs/34 §13):** committed fixture `src/fath/tests/fixtures/expected_schema.json` — a **transcription of the §6.0 pre-bound schema** (every expected value fixed by this plan; nothing pinned from migration output; FATH-V4-002) — compared mechanically against `information_schema`/`pg_catalog` after `upgrade head`. Must prove ALL of: (i) `source_registry.source_id` uuid PRIMARY KEY; (ii) `slug` text NOT NULL + `source_registry_slug_key` UNIQUE; (iii) **no table named `sources` exists** (negative query); (iv) **FK count in schema `public` = 0** (positive count assertion — this slice declares zero FKs; the no-FK-targets-slug query additionally executes and re-runs with content in owning tasks); (v) **exact normalized DB defaults:** `pg_get_expr` = `uuid_generate_v4()` for `source_id`, `'candidate'::text` for `status`, and `adbin IS NULL` for all 31 other columns; (vi) exact column set/types/nullability per the §6.0 table (33 columns, no extras); (vii) constraint set equals exactly the §6.0 list of **13 named constraints**, compared at DEFINITION level per the §6.0 constraint comparison method (extracted enum value sets, numeric bounds, and PK/UNIQUE column lists equal the pre-bound values — not name-only); (viii) index set equals exactly the §6.0 six indexes, compared by **normalized `pg_get_indexdef` string equality against the six pre-bound definition strings** (column, uniqueness, access method, and predicate-absence all bound — FATH-V5-002, not name-only); (ix) **trigger `trg_source_registry_source_id_immutable` present** (BEFORE UPDATE, row-level, enabled) with function `fath_source_id_immutable()`; (x) table set in `public` = {alembic_version, source_registry}. Negative tests (in rolled-back transactions): INSERT with status `bogus` rejected; NULL slug rejected; duplicate slug rejected; duplicate source_id rejected; out-of-range `reliability_prior` (−0.001, 1.001) rejected by CHECK; **UPDATE of `source_id` on an inserted synthetic row raises the trigger exception (FATH-V4-001)** | SPECIFIED oracle (docs/34 §13 + docs 03/24) with plan-bound expected content; independent mechanical comparison (Oracle Policy §7) |
| A5 | **Expected-tree conformance** per §6.1: fixed `git ls-files` scan domain; anchored four-set manifest transcribed from this plan; no unanchored patterns | SPECIFIED base (doc 16) + task-scoped sets from this plan; mechanical script in CI (FATH-P3-004) |
| A6 | `SourceRegistryRecord` (doc-03 contract + `slug` + `status` per docs/34 §§3/5 and docs/24 §1) verified at the contract layer: (i) golden-positive fixture validates; (ii) golden-negatives rejected (missing required field, invalid enum, invalid URL, invalid status, missing slug); (iii) **every doc-03 field default asserted exactly** on an instance constructed without optional fields (e.g. `max_requests_per_minute == 30`, `max_pages_per_cycle == 200`, `max_bytes_per_cycle == 500_000_000`, `robots_status == UNKNOWN`, `auth_requirement == NONE`, `reliability_prior == 0.70`, `strategic_relevance_score == 0.50`, `enabled is True`, `update_frequency_hint == "unknown"`); (iv) **documented numeric boundaries tested at below/at/above** (Oracle Policy §10): `reliability_prior` and `strategic_relevance_score` accept 0 and 1, reject -0.001 and 1.001; `conint(ge=0)` fields accept 0, reject -1. Fixtures obey A13d content rules | SPECIFIED (doc-03 contract; docs/34 §§3/5; docs/24 §1); contract tests (FATH-P3-002 model defaults + boundaries) |
| A7 | ruff (lint+format) and mypy strict pass over `src/` | SPECIFIED (SG-TR-007); CI steps |
| A8 | **Scoped AMENDMENT-001 scan:** no `A100` / Azure OpenAI SDK/endpoint/deployment/env reference in: `src/`, `docker/`, `.github/`, `pyproject.toml`, `uv.lock`, `docker-compose.yml`, `Makefile`, `alembic.ini`, `.env.example`. Exclusions: `docs/`, `.delivery/`, README doc citations | SPECIFIED (docs/33 verifier items 1–2, scoped per its own "implementation, configuration, or sizing" text); scripted CI scan |
| A9 | **Secret scan:** gitleaks (PROPOSED tool) — pinned by exact version AND the sha256 checksum of the downloaded binary recorded and verified in the CI step (a bare version pin of a downloaded artifact is a weaker bind; FATH-V5-004 sweep) — over the candidate tree passes; `.env` git-ignored (test); `.env.example` placeholders only | SPECIFIED intent (doc 28; SG-TR-006) + PROPOSED tooling; CI step + test |
| A10 | CI green on the exact candidate SHA. **Retention per BOOTSTRAP_PLAN §10.6 evidence model (FATH-V4-006): the CI run URL/result is POST-CANDIDATE external evidence — it cannot exist inside the candidate and is never committed to it; the controller records it against the SHA in control-plane state/review records. The criterion is verified by reviewer/controller against that external record** | Review policy §§36–46; Oracle Policy §§44–46; post-candidate record bound to SHA |
| A11 | **Functional extension checks:** create a table with `vector(1024)` column + HNSW index; create an AGE graph + run a minimal cypher query — both succeed (objects created and dropped inside the tests; A4's table-set check runs at step 7, before pytest) | SPECIFIED (docs 22 §1, 02); integration tests |
| A12 | **Zero-seed state at three points (§5):** `source_registry` row count = 0 (a) after fresh `upgrade head`, (b) after the complete pytest session against the same DB (fixture non-persistence proven by observation), (c) after downgrade/upgrade cycle | SPECIFIED boundary (seeds = TASK-006/FA-OPEN-020); CI-level SQL checks independent of pytest (FATH-P3-003) |
| A13 | **No seed content or loader (§5):** (a) authoritative-literal scan zero hits over tracked files — 55 plan-bound literals covering docs 03/24/30 ALL tiers incl. Tier-3; scan-input file at `.github/scan/` is the sole exact-path exemption (definitional; outside fixtures tree); (b) registry-write pattern scan clean outside migrations/tests AND the §5 A13b-ii **AST-allowlist migration DML detector** passes on all real migration files while **failing on every committed negative fixture** — incl. the `op.execute(sa.insert/sa.delete)` and insert-then-delete forms (FATH-V5-001); (c) import discipline holds (DB imports only in connection.py/migrations/tests; `__init__.py` empty/docstring-only; models file pydantic-only); (d) fixture content rules with NO exemption: zero authoritative literals, RFC-2606 domains only, `synthetic_` slug prefix; plus `git ls-files`: no `sources_seed.yaml`, no `*seed*` under `src/`. Marker non-evidentiary. Residual obfuscation limitation stated per Oracle Policy §48; reviewer inspects actual diff | SPECIFIED boundary; mechanical value/content/pattern scans in CI (FATH-P3-003, FATH-V4-003) |
| A14 | **Python runtime:** CI asserts running interpreter is 3.11.x; `pyproject.toml` declares `requires-python = ">=3.11,<3.12"` (verified by test reading pyproject) | PROPOSED baseline (BOOTSTRAP_PLAN §4.4) mechanically enforced once this plan is approved; CI step + test (FATH-P3-002) |
| A15 | **Dependency/environment contract (three sets + versions, §4.9):** REQUIRED ⊆ declared ∧ declared ⊆ (REQUIRED ∪ OPTIONAL) for `[project.dependencies]` and the dev group (PEP-503-normalized exact names — **omission of a REQUIRED dependency FAILS**; FATH-V4-004); **version enforcement (FATH-V5-003):** declared specifiers equal exactly `pydantic>=2,<3`, `pydantic-settings>=2,<3`, `sqlalchemy>=2,<3` AND the resolved `uv.lock` versions for those three packages have major = 2 (mechanical `[[package]]` parse — `uv lock --check` proves lock/metadata consistency, NOT that metadata requires the approved majors); prohibited-name scan over `pyproject.toml` AND `uv.lock` finds none; lock freshness proven by `uv lock --check` (A1) | SPECIFIED contract in this plan grounded in BOOTSTRAP_PLAN §6 dependency discipline + docs/33 (no Azure/LLM deps); scripted CI check (FATH-P3-002, FATH-V4-004, FATH-V5-003) |
| A16 | **Boundary conformance (diff-domain, governed base — FATH-V4-005):** base identity = the exact `main` SHA the task branch was cut from, recorded twice independently: by the controller in BUILD_STATE at dispatch, and by the implementer in `.delivery/evidence/TASK-001/BASE_SHA.txt`; **the reviewer mechanically asserts the two are equal** (closing candidate-controlled-base). CI (identical on push and PR events; fetch-depth 0): assert BASE_SHA is a 40-hex commit ancestor of HEAD (`git merge-base --is-ancestor`); run `git diff --name-status --no-renames <BASE_SHA> HEAD`; FAIL if any line's path is under `docs/` (any status), or any line's path is under `.delivery/` with status ≠ `A` or path not matching `^\.delivery/evidence/TASK-001/` (`--no-renames` forces renames to appear as A+D pairs, so a rename of an existing `.delivery/` file surfaces as a D and fails) | SPECIFIED task boundary (§4.1, §10 — FATH-P3-005, FATH-V4-005); mechanical CI check + reviewer base-equality assertion |

### 6.3 A16 base identity and event behavior (normative detail)

1. At TASK-001 dispatch the controller records `task_base_sha` (the exact `main` commit the task branch is cut from) in BUILD_STATE.
2. The implementer writes that same SHA (40 hex, one line) to `.delivery/evidence/TASK-001/BASE_SHA.txt` — pre-candidate committed evidence (§10).
3. CI behavior is **event-independent**: it never reads PR context or `github.event`; the comparison is always `git diff --name-status --no-renames $(cat .delivery/evidence/TASK-001/BASE_SHA.txt) HEAD` after asserting ancestor status. Push runs and PR runs execute the identical comparison.
4. The independent reviewer asserts `BASE_SHA.txt` == BUILD_STATE `task_base_sha` (mechanical string equality) before crediting A16.

## 7. Implementation sequence (bounded)

1. Scaffold per §4.1 + pyproject (3.11, §4.9 three-set contract) + uv.lock (`uv lock --check` clean) + tooling configs (A1, A7, A14, A15).
2. Postgres Dockerfile with recorded pins + compose + healthchecks; version checks (A2).
3. Settings + `.env.example` (A8/A9 constraints).
4. `db/connection.py` + Alembic wiring + migration 0001 transcribing §6.0 (DDL-only, zero DML, full downgrade, immutability trigger) (A3, A12, A13b-ii).
5. `SourceRegistryRecord` per §4.7 + synthetic contract fixtures per §5 rules (A6, A13d).
6. Transcribe `expected_schema.json` (from §6.0 — including the six pre-bound `pg_get_indexdef` strings and the 13-constraint definition set), `expected_tree.json` (from §6.1), and `.github/scan/authoritative_source_literals.json` (from the §5 55-literal list) (A4, A5, A13a).
7. Remaining tests: extension presence + functional checks (A2, A11), negative schema tests incl. source_id-immutability trigger test (A4), boundary tests (A6), **migration DML detector module + the six §5 negative fixtures, asserting real migrations PASS and every fixture FAILS** (A13b-ii).
8. CI workflow with the full §4.10 step order; verify green on push (A8–A16 in CI).
9. Confirm, Validate, Test — then per the BOOTSTRAP_PLAN §10.6 evidence model: (a) run the full local verification and write all **pre-candidate evidence** (§10 list, including `BASE_SHA.txt`) under `.delivery/evidence/TASK-001/`; (b) create the immutable candidate commit; (c) push — CI runs against the SHA; the **controller** records the CI run URL/result against the exact SHA in control-plane state (post-candidate external evidence — never committed into the candidate; FATH-V4-006).

**What must remain unchanged:** `docs/` (read-only authority — A16); `.delivery/` except additions under `.delivery/evidence/TASK-001/` (A16); no history rewrites; zero rows in `source_registry` anywhere in the candidate (A12).

## 8. Dependencies and blockers

- Dependencies: none (first implementation task). Docker + uv on runner/workstation; no GPU, no external API, no credential.
- Blockers: none for TASK-001. FA-OPEN-020 gates TASK-006 (not this task). FA-OPEN-021 is RESOLVED by AMENDMENT-002 (docs/34). **Merge eligibility** (not implementation or review) additionally depends on GATE-SETUP (§11 / BOOTSTRAP_PLAN §9).

## 9. Stop conditions (stop and surface; do not guess)

1. Pinned AGE or pgvector releases cannot be built/loaded against Postgres 16 → STOP; surface pin options; do not switch Postgres major (doc 28).
2. Any doc-03 field proves irreconcilable with the docs/34 identity model or the §6.0 pre-bound schema proves untranscribable as written → STOP; record; do not invent and do not silently adjust the expected schema (docs/34 is human-approved authority; a §6.0 defect is plan-remediation material, not implementation freedom).
3. Any acceptance criterion can only pass by weakening an oracle → STOP (Oracle Policy §§38–39).
4. Any need for a module path, schema element, event type, or component not defined in the docs folder (or a persisted FK that would target `source_registry(slug)`) → STOP; doc-23 anti-drift and docs/34 §6 require an ADR-style note WITH human (Salim) approval before implementation.
5. CI platform unusable for SHA-bound runs → STOP; surface to controller.

## 10. Test and evidence expectations (FATH-P3-005 + FATH-V4-006 resolution)

- Tests per §6 under `src/fath/tests/`; runnable via pytest in CI (and via the `Makefile` `test` target, which §4.1 discloses); no network access; fixed seeds where randomness exists; fixtures synthetic per §5/A13d.
- **Evidence writer and location (unambiguous):** the **implementer** writes all TASK-001 **pre-candidate** evidence, as new files only, under **`.delivery/evidence/TASK-001/`** — the single authorized `.delivery/` write for this task, included in the allowed-file scope (§4.1) and mechanically enforced by A16. No other `.delivery/` path (`plans/`, `reviews/`, `escalations/`, `audit/`) may be created, modified, or deleted by the candidate. Rationale: Role & Model Policy §8 assigns evidence creation to the implementer; Oracle Policy §46 requires a control-plane-defined location; BOOTSTRAP_PLAN §10.6 defines the retention model.
- **Pre-candidate committed evidence (inside the candidate — derivable from tree content alone, never referencing the candidate SHA):** local test-session transcript; alembic cycle transcript; `pg_extension` listing + version-check outputs (A2); A4 schema-comparison output; A5 tree-check output; A12 zero-row outputs (all three points); A13 scan outputs; A14/A15 outputs; A16 diff listing against BASE_SHA; scoped-scan and gitleaks outputs; tool versions; `BASE_SHA.txt`.
- **Post-candidate external evidence (never inside the candidate — FATH-V4-006):** CI run URL and results for the exact candidate SHA; independent-review records; gate receipts. Retained by the controller in control-plane BUILD_STATE and `.delivery/reviews/**` records committed in LATER commits that never claim membership in the reviewed candidate. The reviewed identity is always the immutable candidate SHA.

## 11. Completion conditions and gate sequencing (FATH-P2-004 — preserved verbatim from V3 in substance)

1. All acceptance criteria A1–A16 pass with recorded evidence (retention split per §10); one immutable candidate commit; candidate SHA reported.
2. Independent implementation review (Sol seat) receives the exact SHA. On APPROVE the candidate state becomes **REVIEW_APPROVED — and stops there.**
3. **Merge eligibility is a separate, later state** and requires, per BOOTSTRAP_PLAN §9 GATE-SETUP and review policy §§36–46, ALL of: (a) branch protection on `main` verified ACTIVE (mechanical GitHub API evidence, not assumption — BUILD_STATE currently records it NOT_VERIFIED); (b) trusted exact-identity verification configured and run against this exact SHA with a recorded receipt (currently NOT_CONFIGURED); (c) BUILD_STATE updated to reflect the verified gate states. There is no review-plus-controller substitute for these gates; until they are verified active, this candidate — like every candidate — remains NOT merge-eligible.
4. No self-approval at any step.

## 12. Plan identity and review

- Plan path: `.delivery/plans/FATH-BOOTSTRAP/TASK-001_PLAN.md` (v6); SHA-256 recorded in the commit/handoff over this exact content.
- Reviewer: `plan-reviewer` (GPT-5.6 Sol, 1M, Max reasoning, fast OFF, read-only, fresh context); outcomes APPROVE / REJECT / BLOCKED; **review sequence 2, attempt 3 of max 3 — FINAL; a rejection becomes BLOCKED_FOR_SALIM** (sequence authorized by Escalation Policy §6 after AMENDMENT-002; seq2 attempt-1 findings adjudicated in `REMEDIATION-3_ADJUDICATION.md`; seq2 attempt-2 findings in `REMEDIATION-4_ADJUDICATION.md`).
